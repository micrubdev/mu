Put a General MIDI SoundFont here as `gm.sf2`.

It is not committed: soundfonts are large and their licences vary. A
freely redistributable one is GeneralUser GS. The client fetches
`/gm.sf2` at startup; without it the browser stays silent and the HUD
still works.
